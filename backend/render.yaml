services:
  - type: web
    name: schedule-api
    runtime: dotnet
    rootDir: backend
    buildCommand: dotnet restore && dotnet publish -c Release -o ./publish
    startCommand: dotnet ./publish/ScheduleManager.dll
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production